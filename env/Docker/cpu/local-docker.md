local Docker   
====   
# docker run
    $ docker run --name miyo -ti -d bvlc/caffe:cpu
        -t terminal
        -i interractive
        -d background
# docker login
    $ docker exec -it <name> bash
